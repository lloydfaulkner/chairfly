{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash(git commit *)",
        "hooks": [
          {
            "type": "command",
            "command": "echo '{\"systemMessage\": \"💡 Docs sync: Run the chairfly-sync-docs skill to check if CLAUDE.md or skills need updating.\"}'",
            "statusMessage": "Checking docs sync reminder"
          }
        ]
      }
    ]
  }
}
